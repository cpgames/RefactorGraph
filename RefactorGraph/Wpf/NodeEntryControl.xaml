<UserControl x:Class="RefactorGraph.NodeEntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RefactorGraph"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="200"
             Name="MyNodeEntryControl">
    <Label Name="Label"
           Content="{Binding  Path=NodeType, RelativeSource={RelativeSource AncestorType={x:Type local:NodeEntryControl}}}" Margin="5,0,0,0">
        <Label.Style>
            <Style TargetType="Label">
                <Setter Property="Foreground" Value="White" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Foreground" Value="Orange" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Label.Style>
    </Label>
</UserControl>